/**
 * Edge Node API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import NodeGroup from './NodeGroup';

/**
 * The UpdateEdgeNodeResponse model module.
 * @module model/UpdateEdgeNodeResponse
 * @version 1.0.0
 */
class UpdateEdgeNodeResponse {
    /**
     * Constructs a new <code>UpdateEdgeNodeResponse</code>.
     * @alias module:model/UpdateEdgeNodeResponse
     * @param groups {Array.<module:model/NodeGroup>} 
     * @param id {Number} 
     * @param name {String} 
     * @param status {String} 
     */
    constructor(groups, id, name, status) { 
        
        UpdateEdgeNodeResponse.initialize(this, groups, id, name, status);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, groups, id, name, status) { 
        obj['groups'] = groups;
        obj['id'] = id;
        obj['name'] = name;
        obj['status'] = status;
    }

    /**
     * Constructs a <code>UpdateEdgeNodeResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UpdateEdgeNodeResponse} obj Optional instance to populate.
     * @return {module:model/UpdateEdgeNodeResponse} The populated <code>UpdateEdgeNodeResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UpdateEdgeNodeResponse();

            if (data.hasOwnProperty('groups')) {
                obj['groups'] = ApiClient.convertToType(data['groups'], [NodeGroup]);
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UpdateEdgeNodeResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UpdateEdgeNodeResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of UpdateEdgeNodeResponse.RequiredProperties) {
            if (!data[property]) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['groups']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['groups'])) {
                throw new Error("Expected the field `groups` to be an array in the JSON data but got " + data['groups']);
            }
            // validate the optional field `groups` (array)
            for (const item of data['groups']) {
                NodeGroup.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }

        return true;
    }


}

UpdateEdgeNodeResponse.RequiredProperties = ["groups", "id", "name", "status"];

/**
 * @member {Array.<module:model/NodeGroup>} groups
 */
UpdateEdgeNodeResponse.prototype['groups'] = undefined;

/**
 * @member {Number} id
 */
UpdateEdgeNodeResponse.prototype['id'] = undefined;

/**
 * @member {String} name
 */
UpdateEdgeNodeResponse.prototype['name'] = undefined;

/**
 * @member {String} status
 */
UpdateEdgeNodeResponse.prototype['status'] = undefined;






export default UpdateEdgeNodeResponse;

