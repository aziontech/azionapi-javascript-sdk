/**
 * Edgenode API
 * Azion Orchestration
 *
 * The version of the OpenAPI document: 2.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import UnauthorizedEdgeNodeInfo from './UnauthorizedEdgeNodeInfo';

/**
 * The AuthorizeEdgeNodesResponse model module.
 * @module model/AuthorizeEdgeNodesResponse
 * @version 2.0.0
 */
class AuthorizeEdgeNodesResponse {
    /**
     * Constructs a new <code>AuthorizeEdgeNodesResponse</code>.
     * @alias module:model/AuthorizeEdgeNodesResponse
     * @param authorized {Array.<Number>} 
     * @param errors {Array.<module:model/UnauthorizedEdgeNodeInfo>} 
     */
    constructor(authorized, errors) { 
        
        AuthorizeEdgeNodesResponse.initialize(this, authorized, errors);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, authorized, errors) { 
        obj['authorized'] = authorized;
        obj['errors'] = errors;
    }

    /**
     * Constructs a <code>AuthorizeEdgeNodesResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/AuthorizeEdgeNodesResponse} obj Optional instance to populate.
     * @return {module:model/AuthorizeEdgeNodesResponse} The populated <code>AuthorizeEdgeNodesResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new AuthorizeEdgeNodesResponse();

            if (data.hasOwnProperty('authorized')) {
                obj['authorized'] = ApiClient.convertToType(data['authorized'], ['Number']);
            }
            if (data.hasOwnProperty('errors')) {
                obj['errors'] = ApiClient.convertToType(data['errors'], [UnauthorizedEdgeNodeInfo]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>AuthorizeEdgeNodesResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>AuthorizeEdgeNodesResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of AuthorizeEdgeNodesResponse.RequiredProperties) {
            if (!data[property]) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['authorized'])) {
            throw new Error("Expected the field `authorized` to be an array in the JSON data but got " + data['authorized']);
        }
        if (data['errors']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['errors'])) {
                throw new Error("Expected the field `errors` to be an array in the JSON data but got " + data['errors']);
            }
            // validate the optional field `errors` (array)
            for (const item of data['errors']) {
                UnauthorizedEdgeNodeInfo.validateJSON(item);
            };
        }

        return true;
    }


}

AuthorizeEdgeNodesResponse.RequiredProperties = ["authorized", "errors"];

/**
 * @member {Array.<Number>} authorized
 */
AuthorizeEdgeNodesResponse.prototype['authorized'] = undefined;

/**
 * @member {Array.<module:model/UnauthorizedEdgeNodeInfo>} errors
 */
AuthorizeEdgeNodesResponse.prototype['errors'] = undefined;






export default AuthorizeEdgeNodesResponse;

