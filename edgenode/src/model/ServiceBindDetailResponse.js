/**
 * Edgenode API
 * Azion Orchestration
 *
 * The version of the OpenAPI document: 2.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import Variable from './Variable';

/**
 * The ServiceBindDetailResponse model module.
 * @module model/ServiceBindDetailResponse
 * @version 2.0.0
 */
class ServiceBindDetailResponse {
    /**
     * Constructs a new <code>ServiceBindDetailResponse</code>.
     * @alias module:model/ServiceBindDetailResponse
     * @param id {Number} 
     * @param serviceId {Number} 
     * @param serviceName {String} 
     * @param variables {Array.<module:model/Variable>} 
     */
    constructor(id, serviceId, serviceName, variables) { 
        
        ServiceBindDetailResponse.initialize(this, id, serviceId, serviceName, variables);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, serviceId, serviceName, variables) { 
        obj['id'] = id;
        obj['service_id'] = serviceId;
        obj['service_name'] = serviceName;
        obj['variables'] = variables;
    }

    /**
     * Constructs a <code>ServiceBindDetailResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ServiceBindDetailResponse} obj Optional instance to populate.
     * @return {module:model/ServiceBindDetailResponse} The populated <code>ServiceBindDetailResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ServiceBindDetailResponse();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('service_id')) {
                obj['service_id'] = ApiClient.convertToType(data['service_id'], 'Number');
            }
            if (data.hasOwnProperty('service_name')) {
                obj['service_name'] = ApiClient.convertToType(data['service_name'], 'String');
            }
            if (data.hasOwnProperty('variables')) {
                obj['variables'] = ApiClient.convertToType(data['variables'], [Variable]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ServiceBindDetailResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ServiceBindDetailResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ServiceBindDetailResponse.RequiredProperties) {
            if (!data[property]) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['service_name'] && !(typeof data['service_name'] === 'string' || data['service_name'] instanceof String)) {
            throw new Error("Expected the field `service_name` to be a primitive type in the JSON string but got " + data['service_name']);
        }
        if (data['variables']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['variables'])) {
                throw new Error("Expected the field `variables` to be an array in the JSON data but got " + data['variables']);
            }
            // validate the optional field `variables` (array)
            for (const item of data['variables']) {
                Variable.validateJSON(item);
            };
        }

        return true;
    }


}

ServiceBindDetailResponse.RequiredProperties = ["id", "service_id", "service_name", "variables"];

/**
 * @member {Number} id
 */
ServiceBindDetailResponse.prototype['id'] = undefined;

/**
 * @member {Number} service_id
 */
ServiceBindDetailResponse.prototype['service_id'] = undefined;

/**
 * @member {String} service_name
 */
ServiceBindDetailResponse.prototype['service_name'] = undefined;

/**
 * @member {Array.<module:model/Variable>} variables
 */
ServiceBindDetailResponse.prototype['variables'] = undefined;






export default ServiceBindDetailResponse;

