/**
 * Edgenode API
 * Azion Orchestration
 *
 * The version of the OpenAPI document: 2.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import NodeGroup from './NodeGroup';

/**
 * The EdgeNodeResponse model module.
 * @module model/EdgeNodeResponse
 * @version 2.0.0
 */
class EdgeNodeResponse {
    /**
     * Constructs a new <code>EdgeNodeResponse</code>.
     * @alias module:model/EdgeNodeResponse
     * @param id {Number} 
     */
    constructor(id) { 
        
        EdgeNodeResponse.initialize(this, id);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id) { 
        obj['id'] = id;
    }

    /**
     * Constructs a <code>EdgeNodeResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/EdgeNodeResponse} obj Optional instance to populate.
     * @return {module:model/EdgeNodeResponse} The populated <code>EdgeNodeResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new EdgeNodeResponse();

            if (data.hasOwnProperty('groups')) {
                obj['groups'] = ApiClient.convertToType(data['groups'], [NodeGroup]);
            }
            if (data.hasOwnProperty('hash_id')) {
                obj['hash_id'] = ApiClient.convertToType(data['hash_id'], 'String');
            }
            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'Number');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('status')) {
                obj['status'] = ApiClient.convertToType(data['status'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>EdgeNodeResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>EdgeNodeResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of EdgeNodeResponse.RequiredProperties) {
            if (!data[property]) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['groups']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['groups'])) {
                throw new Error("Expected the field `groups` to be an array in the JSON data but got " + data['groups']);
            }
            // validate the optional field `groups` (array)
            for (const item of data['groups']) {
                NodeGroup.validateJSON(item);
            };
        }
        // ensure the json data is a string
        if (data['hash_id'] && !(typeof data['hash_id'] === 'string' || data['hash_id'] instanceof String)) {
            throw new Error("Expected the field `hash_id` to be a primitive type in the JSON string but got " + data['hash_id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['status'] && !(typeof data['status'] === 'string' || data['status'] instanceof String)) {
            throw new Error("Expected the field `status` to be a primitive type in the JSON string but got " + data['status']);
        }

        return true;
    }


}

EdgeNodeResponse.RequiredProperties = ["id"];

/**
 * @member {Array.<module:model/NodeGroup>} groups
 */
EdgeNodeResponse.prototype['groups'] = undefined;

/**
 * @member {String} hash_id
 */
EdgeNodeResponse.prototype['hash_id'] = undefined;

/**
 * @member {Number} id
 */
EdgeNodeResponse.prototype['id'] = undefined;

/**
 * @member {String} name
 */
EdgeNodeResponse.prototype['name'] = undefined;

/**
 * @member {String} status
 */
EdgeNodeResponse.prototype['status'] = undefined;






export default EdgeNodeResponse;

