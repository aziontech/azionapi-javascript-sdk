/**
 * Purge API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The PurgeCacheKeyRequest model module.
 * @module model/PurgeCacheKeyRequest
 * @version 1.0.0
 */
class PurgeCacheKeyRequest {
    /**
     * Constructs a new <code>PurgeCacheKeyRequest</code>.
     * @alias module:model/PurgeCacheKeyRequest
     * @param urls {Array.<String>} 
     * @param method {String} 
     * @param layer {String} 
     */
    constructor(urls, method, layer) { 
        
        PurgeCacheKeyRequest.initialize(this, urls, method, layer);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, urls, method, layer) { 
        obj['urls'] = urls;
        obj['method'] = method;
        obj['layer'] = layer;
    }

    /**
     * Constructs a <code>PurgeCacheKeyRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PurgeCacheKeyRequest} obj Optional instance to populate.
     * @return {module:model/PurgeCacheKeyRequest} The populated <code>PurgeCacheKeyRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PurgeCacheKeyRequest();

            if (data.hasOwnProperty('urls')) {
                obj['urls'] = ApiClient.convertToType(data['urls'], ['String']);
            }
            if (data.hasOwnProperty('method')) {
                obj['method'] = ApiClient.convertToType(data['method'], 'String');
            }
            if (data.hasOwnProperty('layer')) {
                obj['layer'] = ApiClient.convertToType(data['layer'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PurgeCacheKeyRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PurgeCacheKeyRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of PurgeCacheKeyRequest.RequiredProperties) {
            if (!data[property]) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is an array
        if (!Array.isArray(data['urls'])) {
            throw new Error("Expected the field `urls` to be an array in the JSON data but got " + data['urls']);
        }
        // ensure the json data is a string
        if (data['method'] && !(typeof data['method'] === 'string' || data['method'] instanceof String)) {
            throw new Error("Expected the field `method` to be a primitive type in the JSON string but got " + data['method']);
        }
        // ensure the json data is a string
        if (data['layer'] && !(typeof data['layer'] === 'string' || data['layer'] instanceof String)) {
            throw new Error("Expected the field `layer` to be a primitive type in the JSON string but got " + data['layer']);
        }

        return true;
    }


}

PurgeCacheKeyRequest.RequiredProperties = ["urls", "method", "layer"];

/**
 * @member {Array.<String>} urls
 */
PurgeCacheKeyRequest.prototype['urls'] = undefined;

/**
 * @member {String} method
 */
PurgeCacheKeyRequest.prototype['method'] = undefined;

/**
 * @member {String} layer
 */
PurgeCacheKeyRequest.prototype['layer'] = undefined;






export default PurgeCacheKeyRequest;

