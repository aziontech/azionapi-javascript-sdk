/**
 * Object Storage
 * REST API OpenAPI documentation for the Object Storage
 *
 * The version of the OpenAPI document: 1.0.0 (v1)
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import BucketObject from './BucketObject';

/**
 * The PaginatedBucketObjectList model module.
 * @module model/PaginatedBucketObjectList
 * @version 1.0.0 (v1)
 */
class PaginatedBucketObjectList {
    /**
     * Constructs a new <code>PaginatedBucketObjectList</code>.
     * @alias module:model/PaginatedBucketObjectList
     */
    constructor() { 
        
        PaginatedBucketObjectList.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>PaginatedBucketObjectList</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/PaginatedBucketObjectList} obj Optional instance to populate.
     * @return {module:model/PaginatedBucketObjectList} The populated <code>PaginatedBucketObjectList</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new PaginatedBucketObjectList();

            if (data.hasOwnProperty('count')) {
                obj['count'] = ApiClient.convertToType(data['count'], 'Number');
            }
            if (data.hasOwnProperty('next')) {
                obj['next'] = ApiClient.convertToType(data['next'], 'String');
            }
            if (data.hasOwnProperty('previous')) {
                obj['previous'] = ApiClient.convertToType(data['previous'], 'String');
            }
            if (data.hasOwnProperty('continuation_token')) {
                obj['continuation_token'] = ApiClient.convertToType(data['continuation_token'], 'String');
            }
            if (data.hasOwnProperty('results')) {
                obj['results'] = ApiClient.convertToType(data['results'], [BucketObject]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>PaginatedBucketObjectList</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>PaginatedBucketObjectList</code>.
     */
    static validateJSON(data) {
        // ensure the json data is a string
        if (data['next'] && !(typeof data['next'] === 'string' || data['next'] instanceof String)) {
            throw new Error("Expected the field `next` to be a primitive type in the JSON string but got " + data['next']);
        }
        // ensure the json data is a string
        if (data['previous'] && !(typeof data['previous'] === 'string' || data['previous'] instanceof String)) {
            throw new Error("Expected the field `previous` to be a primitive type in the JSON string but got " + data['previous']);
        }
        // ensure the json data is a string
        if (data['continuation_token'] && !(typeof data['continuation_token'] === 'string' || data['continuation_token'] instanceof String)) {
            throw new Error("Expected the field `continuation_token` to be a primitive type in the JSON string but got " + data['continuation_token']);
        }
        if (data['results']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['results'])) {
                throw new Error("Expected the field `results` to be an array in the JSON data but got " + data['results']);
            }
            // validate the optional field `results` (array)
            for (const item of data['results']) {
                BucketObject.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * @member {Number} count
 */
PaginatedBucketObjectList.prototype['count'] = undefined;

/**
 * @member {String} next
 */
PaginatedBucketObjectList.prototype['next'] = undefined;

/**
 * @member {String} previous
 */
PaginatedBucketObjectList.prototype['previous'] = undefined;

/**
 * @member {String} continuation_token
 */
PaginatedBucketObjectList.prototype['continuation_token'] = undefined;

/**
 * @member {Array.<module:model/BucketObject>} results
 */
PaginatedBucketObjectList.prototype['results'] = undefined;






export default PaginatedBucketObjectList;

